-- 📄 Configuration.lua
return {
    -- การตั้งค่าออโต้ฟาร์ม
    AutoFarm = {
        Enabled = false,           -- เปิด/ปิดระบบ
        AttackRange = 50,          -- ระยะโจมตี
        AttackDelay = 0.5,         -- ความเร็วโจมตี
        SearchRadius = 500,        -- รัศมีค้นหาศัตรู
        TargetFilter = {           -- ตัวกรองเป้าหมาย
            "Bandit",              -- โจร
            "Monkey",              -- ลิง
            "Pirate"               -- โจรสลัด
        }
    },
    
    -- การตั้งค่าระบบเคลื่อนที่
    Teleport = {
        UseSafeTeleport = true,    -- ใช้การเคลื่อนที่ปลอดภัย
        TeleportDelay = 1,         -- หน่วงเวลาก่อนเคลื่อนที่
        AntiAfk = true             -- ป้องกันการถูก AFK
    },
    
    -- การตั้งค่าระบบต่อสู้
    Combat = {
        AutoClick = true,          -- คลิกอัตโนมัติ
        UseSkills = true,          -- ใช้สกิล
        SkillPriority = {          -- ลำดับการใช้สกิล
            "Z", "X", "C", "V", "F"
        }
    },
    
    -- การตั้งค่า UI
    UI = {
        Theme = "Dark",            -- Dark, Light, Blue
        Position = "Right",        -- ตำแหน่ง UI
        Opacity = 0.9,             -- ความโปร่งใส
        Keybind = Enum.KeyCode.RightControl  -- ปุ่มลัด
    }
}
